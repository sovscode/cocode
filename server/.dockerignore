# Dependency directories
node_modules

# Next.js build output
.next
out
build

# Environment variables (prevents leaking local secrets into the image)
.env*

# Git and version control
.git
.gitignore

# Docker files
Dockerfile
.dockerignore

# Vercel / deployment specific
.vercel

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Misc text files (optional, but keeps the image slightly smaller)
README.md
